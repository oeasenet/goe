APP_NAME=DBDemo
GOE_ENV=dev

# Database Configuration (SQLite in-memory for easy demo)
DB_DRIVER=sqlite
DB_DATABASE=./db_demo.db # Use a file for this demo to show persistence
DB_LOG_MODE=true
# DB_AUTO_MIGRATE=true # We'll call migrate explicitly in the demo
